# Dependencies required for installation (keep in sync with setup.py)
requests>=2.16.2
six >= 1.12.0
stone>=2,<3.3.3
# Other dependencies for development
ply
pytest
pytest-runner==6.0.1
sphinx
sphinx_rtd_theme
twine
wheel
